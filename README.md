# CountESS Docker Image

Docker image for running CountESS command-line workflows in Galaxy.

The image installs CountESS from PyPI and provides `countess_cmd`.

```bash
docker run --rm quay.io/galaxy/countess:0.1.26 countess_cmd --version
```
